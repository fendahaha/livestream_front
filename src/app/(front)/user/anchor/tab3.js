import GiftSendList from "@/app/(front)/user/anchor/GiftSendList";

export default function Tab3({anchor}) {
    return (
        <div>
            <GiftSendList anchor={anchor}/>
        </div>
    );
}